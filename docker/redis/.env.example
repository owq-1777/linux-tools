# Example env for docker/redis/docker-compose.yaml
# Copy to .env and set a strong password before starting.

# Redis Stack server image (optional override)
REDIS_IMAGE=redis/redis-stack-server:7.4.0-v8

# Required: set a strong password for Redis AUTH
REDIS_PASSWORD=ChangeMePlease
